# Hoist shared devDependencies to the root node_modules
# so turbo, typescript, etc. are available to all workspace members.
hoist=true

# Ensure pnpm does not prompt for confirmation during CI
auto-install-peers=true

# Strict peer dependency resolution
strict-peer-dependencies=false
