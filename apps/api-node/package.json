{
  "name": "@wc3-coach/api-node",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint: no linter configured yet\"",
    "test": "echo \"test: no tests yet (TODO T1.3 integration)\"",
    "dev": "node --watch dist/server.js",
    "worker": "node --watch dist/worker.js",
    "start": "node dist/server.js",
    "start:worker": "node dist/worker.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@wc3-coach/db": "workspace:*",
    "@wc3-coach/parser": "workspace:*",
    "@wc3-coach/shared-types": "workspace:*",
    "bullmq": "^5.53.3",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "*"
  }
}
